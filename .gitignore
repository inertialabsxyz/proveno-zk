/target
.env
*.bin
!contracts/test/fixtures/*.bin
contracts/broadcast/

# Transpiled OpenVM guest artifacts (cargo openvm build output)
/openvm

# OpenVM app proofs (build output of `make prove-openvm`)
*.app.proof

# nargo/bb build output, regenerated by `make test-prove` and `proveno-noir`.
# Checked in by accident once; nothing consumes these copies (the contracts
# fixtures under contracts/test/fixtures/*.bin are the committed artifacts).
/noir/target
/noir/Prover.toml
