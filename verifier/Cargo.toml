[package]
name = "luai-verifier"
version = "0.1.0"
edition = "2024"

[dependencies]
luai = { path = "..", features = ["zkvm"] }
sha2 = { version = "0.10", default-features = false, features = ["oid"] }
